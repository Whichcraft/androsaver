# TODO

- [ ] Replace the bundled slideshow example images with a curated set of public-domain photos before release.
- [ ] Migrate `psysuals` from the current Git submodule to a Git subtree: add the upstream remote, fetch the latest psysualizer release, import it under `psysuals/`, reconcile and backport all upstream changes into the Android visualizer port, update port notes/changelog/docs, and verify the migration through GitHub CI without running a local Android build.
